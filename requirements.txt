# NIFTY Bearnness Screener - Production Requirements
# Python 3.12+

# Data fetching & processing
yfinance>=0.2.28
pandas>=2.0.0
numpy>=1.24.0

# Technical analysis
ta-lib>=0.4.28
# Alternative if ta-lib installation fails:
# talib-binary>=0.4.24

# API interactions
requests>=2.31.0
python-dotenv>=1.0.0

# Data storage
sqlalchemy>=2.0.0
sqlite3  # Built-in, no need to install

# Threading & async
# (Standard library - requests included)

# HTML/Report generation
jinja2>=3.1.0

# Development/Optional
pytest>=7.4.0  # For testing
black>=23.0.0  # Code formatting
mypy>=1.0.0    # Type checking

# Database
# sqlite3 is built-in, no need to install separately
